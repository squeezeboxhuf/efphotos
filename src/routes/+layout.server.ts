import { db } from "$lib/server/db"
import { years } from "$lib/server/db/schema"

// export const load = async () => {
//   const yearsData = await db.select().from(years)

//   return { years: yearsData }
// }